<!DOCTYPE html>
<html lang="en">
<head>
  <link href="//netdna.bootstrapcdn.com/twitter-bootstrap/2.3.1/css/bootstrap-combined.min.css" rel="stylesheet">
  <style type="text/css">
      body {
        padding-top: 40px;
        padding-bottom: 40px;
        background-color: #f5f5f5;
      }

      h1, h3, p {
        text-align: center;
      }

      .form-upload {
        max-width: 300px;
        padding: 19px 29px 29px;
        margin: 0 auto 20px;
        background-color: #fff;
        border: 1px solid #e5e5e5;
        -webkit-border-radius: 5px;
           -moz-border-radius: 5px;
                border-radius: 5px;
        -webkit-box-shadow: 0 1px 2px rgba(0,0,0,.05);
           -moz-box-shadow: 0 1px 2px rgba(0,0,0,.05);
                box-shadow: 0 1px 2px rgba(0,0,0,.05);
      }

      .form-upload {
        margin-bottom: 10px;
      }

      .btn-large {
        width: 100%;
        box-sizing: border-box;
      }
    </style>
</head>
<body>
  <div class="container">
    <h1>Search Words</h1>
    <form id="upload" class="form-upload" method="post" enctype="multipart/form-data">
      <% if defined? @error %><div class="alert alert-error"><%= @error %></div><% end %>
      <h3 class="form-upload-heading">Upload a CSV</h3>
      <p>Do not include column headers.<br/>Your CSV file should follow this format:</p>
      <pre>"A search query", 100
"Another search query", 200
"A third search query", 300</pre>
      <div class="well">
        <input type="file" name="file" />
      </div>
      <button id="submit" class="btn btn-large btn-primary" type="submit">Process</button>
      <% if @link %>
      <hr />
      <h3>Your file is ready!</h3>
      <a class="file-download btn btn-large btn-primary" href="<%= @link %>" target="_blank">Download processed file.</a>
      <% end %>
    </form>
  </div>
</body>
</html>
