<!DOCTYPE html>
<html lang="en">
<head>
  <link href="//netdna.bootstrapcdn.com/twitter-bootstrap/2.3.1/css/bootstrap-combined.min.css" rel="stylesheet">
  <style type="text/css">
      body {
        padding-top: 40px;
        padding-bottom: 40px;
        background-color: #f5f5f5;
      }

      .form-upload {
        max-width: 300px;
        padding: 19px 29px 29px;
        margin: 0 auto 20px;
        background-color: #fff;
        border: 1px solid #e5e5e5;
        -webkit-border-radius: 5px;
           -moz-border-radius: 5px;
                border-radius: 5px;
        -webkit-box-shadow: 0 1px 2px rgba(0,0,0,.05);
           -moz-box-shadow: 0 1px 2px rgba(0,0,0,.05);
                box-shadow: 0 1px 2px rgba(0,0,0,.05);
      }
      .form-upload .form-upload-heading,
      .form-signin .checkbox {
        margin-bottom: 10px;
      }
      .form-upload input[type="text"],
      .form-upload input[type="password"] {
        font-size: 16px;
        height: auto;
        margin-bottom: 15px;
        padding: 7px 9px;
      }
    </style>
</head>
<body>
  <div id="dropbox" class="container">
    <div class="message"></div>
    <form id="upload" class="form-upload" method="post" enctype="multipart/form-data">
      <h2 class="form-upload-heading">Upload a CSV</h2>
      <input type="file" name="file" />
      <button id="submit" class="btn btn-large btn-primary" type="submit">Upload</button>
      <% if @link %>
      <hr />
      <h3>Huzzah, your file is ready!</h3>
      <a class="file-download btn" href="<%= @link %>" target="_blank">Download processed file.</a>
      <% end %>
    </form>
  </div>
</body>
</html>
